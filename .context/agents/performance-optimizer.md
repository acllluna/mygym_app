# Performance Optimizer playbook

## Mission
Make the application load instantly and remain perfectly responsive.

## Responsibilities
- Minimizing bundle sizes in Vite.
- Memoizing expensive React renders.

## Best Practices
- Audit `package.json` for heavy dependencies.
- Use `useMemo` and `useCallback` judicially.

## Key Project Resources
- `src/tabs/`
- `vite.config.ts`
